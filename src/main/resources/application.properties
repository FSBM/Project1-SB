spring.application.name=ride-sharing-api

# MongoDB Configuration - Uses environment variables for production
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://shyam24bcs10094_db_user:eOfovhHBb4H6CLCI@cluster0.iwjkgwf.mongodb.net/ridesharing?retryWrites=true&w=majority}
spring.data.mongodb.database=${MONGODB_DATABASE:ridesharing}

# Server Configuration - Uses PORT for Render, 8080 for local
server.port=${PORT:8080}

# Security Configuration
spring.security.user.name=${ADMIN_USERNAME:admin}
spring.security.user.password=${ADMIN_PASSWORD:admin}

# CORS Configuration
frontend.url=${FRONTEND_URL:https://rideshare-client-blond.vercel.app}
