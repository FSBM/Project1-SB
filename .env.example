# Environment Variables for Render Deployment
# DO NOT set PORT - Render sets it automatically
# DO NOT set SERVER_PORT - application.properties uses ${PORT:8080}

# MongoDB Configuration (MUST use SPRING_DATA_MONGODB_URI, not MONGODB_URI)
SPRING_DATA_MONGODB_URI=mongodb+srv://shyam24bcs10094_db_user:eOfovhHBb4H6CLCI@cluster0.iwjkgwf.mongodb.net/ridesharing?retryWrites=true&w=majority
MONGODB_DATABASE=ridesharing

# Admin Credentials (CHANGE IN PRODUCTION!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Frontend URL for CORS
FRONTEND_URL=https://rideshare-client-blond.vercel.app

